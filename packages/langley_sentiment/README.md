# langley_sentiment — Sentiment & Psychology Agent (placeholder)

> 🅿️ **Not implemented yet.** This is a reserved home in the monorepo.

**Job:** Measure crowd emotion, whale activity, and FOMO/FUD cycles.

Built after the Risk Guardian (`langley_risk`) clears its eval bar, following the same pattern: `domain/`, `providers/`, `tools/`, `agents/`, `service/` (with an authoritative deterministic gate), and `evals/` + `tests/`.

See [`packages/langley_risk`](../langley_risk) for the reference implementation and [`docs/architecture.md`](../../docs/architecture.md) for the roadmap.
