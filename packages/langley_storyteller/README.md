# langley_storyteller — Storyteller Agent (placeholder)

> 🅿️ **Not implemented yet.** This is a reserved home in the monorepo.

**Job:** Convert technical findings into compelling, shareable narratives (and clean PDF summaries).

Built after the specialist agents, following the same pattern: `domain/`, `agents/`, `service/`, and `evals/` + `tests/`. (It consumes structured agent output rather than external data, so it may not need a `providers/` layer.)

See [`packages/langley_risk`](../langley_risk) for the reference implementation and [`docs/architecture.md`](../../docs/architecture.md) for the roadmap.
